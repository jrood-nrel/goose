modules:
  default:
    enable: [tcl]
    tcl:
      hash_length: 0
      projections:
        all: '{compiler.name}-{compiler.version}/{name}/{version}/{hash:4}'
      all:
        conflict:
          - '{name}'
        environment:
          set:
            '{name}_ROOT_DIR': '{prefix}'
